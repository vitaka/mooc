# ¡ATENCIÓN! Este código no funciona, es solo un ejemplo
usuario = input('Dame el nombre del usuario: ')
password = input('Dame la contraseña: ')

# Esta función no está definida y por eso este código no funciona, es solo un ejemplo.
validar_usuario(usuario, password)